[settings]
experimental=true

[tools]
java = "17"

[env]
_.file = ".env"

[tasks.verify-fmt]
silent=true
run = "./gradlew spotlessCheck"

[hooks]
postinstall = "mise generate git-pre-commit --write --task=verify-fmt"
